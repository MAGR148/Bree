<%= form_with(model: invitation, html: {class: 'space-y-6' }) do |form| %>
  <% if invitation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(invitation.errors.count, "error") %> prohibited this invitation from being saved:</h2>

      <ul>
        <% invitation.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, class: 'block text-sm font-medium text-gray-700' %>
    <div class="mt-1">
      <%= form.text_field :name, class: 'appearance-none block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm placeholder-gray-400 focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm' %>
    </div> 
  </div>

  <div>
    <%= form.label :email, class: 'block text-sm font-medium text-gray-700' %>
    <div class="mt-1">
      <%= form.text_field :email, class: 'appearance-none block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm placeholder-gray-400 focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm' %>
    </div>
  </div>


  <div>
    <%= form.label :city, class: 'block text-sm font-medium text-gray-700' %>
    <div class="mt-1">
      <%= form.text_field :city, class: 'appearance-none block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm placeholder-gray-400 focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm' %>
    </div>
  </div>

  <div class="flex items-center justify-between">
    <div class="flex items-center">
  
    <%= form.check_box :nutrition_professional, class: 'h-4 w-4 text-indigo-600 focus:ring-indigo-500 border-gray-300 rounded' %>
    <%= form.label :nutrition_professional, class: 'ml-2 block text-sm text-gray-900' %>    
  </div>

      
  </div>
  <div>
    <%= form.submit 'Solicitar invitación', class: 'w-full flex justify-center py-2 px-4 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-purple-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500' %>
  </div>
<% end %>
